<script>
    window.ui = {
        title: "{{ site.title }}",
        baseurl: "{{ site.baseurl }}",
        i18n: {
            search_results: "{{ __.search_results | default: '搜索结果' }}",
            search_results_found: "{{ __.search_results_found | default: '搜索完成, 发现 # 个链接 匹配' }}",
            search_results_not_found: "{{ __.search_results_not_found | default: '没找到相应的结果' }}"
        }
    };
</script>
